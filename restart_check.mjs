import Database from "better-sqlite3"; const db = new Database("database.sqlite"); const total = db.prepare("SELECT COUNT(*) as count FROM bot_logs WHERE userId = 1").get(); const latest = db.prepare("SELECT level, message FROM bot_logs WHERE userId = 1 ORDER BY id DESC LIMIT 5").all(); console.log(`Total logs: ${total.count}`); console.log("Latest logs after restart:"); latest.forEach((log, i) => console.log(`${i+1}. [${log.level}] ${log.message.substring(0,120)}`)); db.close();

import Database from "better-sqlite3"; const db = new Database("database.sqlite"); const latest = db.prepare("SELECT level, message, createdAt FROM bot_logs WHERE userId = 1 AND (message LIKE \"%Analyzing%\" OR message LIKE \"%indicators calculated%\" OR message LIKE \"%signal found%\") ORDER BY id DESC LIMIT 5").all(); console.log("Analysis logs found:"); if (latest.length === 0) { console.log("No detailed analysis logs yet"); } else { latest.forEach((log, i) => console.log(`${i+1}. [${log.level}] ${log.message}`)); } db.close();
